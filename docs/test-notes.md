# Test Notes

This file should be IGNORED by the review because `docs/**` is in ignored paths.

## Known Issues

- var usage everywhere
- SQL injection in getUserById
- Hardcoded API key

If this file gets reviewed, the ignored paths setting is NOT working.
