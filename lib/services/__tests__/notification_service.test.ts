import { NotificationService } from '../notification_service';import { FirebaseFirestore } from '@firebase/firestore-types';import { FirebaseMessaging } from '@firebase/messaging-types';jest.mock('@firebase/firestore-types');jest.mock('@firebase/messaging-types');describe('NotificationService', () => {  let notificationService: NotificationService;  let mockFirestore: jest.Mocked<FirebaseFirestore>;  let mockMessaging: jest.Mocked<FirebaseMessaging>;  beforeEach(() => {    mockFirestore = {      collection: jest.fn().mockReturnThis(),      doc: jest.fn().mockReturnThis(),      runTransaction: jest.fn(),      where: jest.fn().mockReturnThis(),      orderBy: jest.fn().mockReturnThis(),      limit: jest.fn().mockReturnThis(),      get: jest.fn(),    } as any;    mockMessaging = {      getToken: jest.fn().mockResolvedValue('mock-token'),    } as any;    notificationService = new NotificationService({      firestore: mockFirestore,      messaging: mockMessaging,    });  });  it('registers token correctly', async () => {    mockFirestore.runTransaction.mockImplementation(async (callback) => {      await callback({        update: jest.fn(),      });    });    const result = await notificationService.registerToken('user123');    expect(result).toBe(true);    expect(mockMessaging.getToken).toHaveBeenCalled();    expect(mockFirestore.runTransaction).toHaveBeenCalled();  });  it('handles token registration failure', async () => {    mockMessaging.getToken.mockResolvedValue(null);    const result = await notificationService.registerToken('user123');    expect(result).toBe(false);  });  it('gets recent notifications correctly', async () => {    const mockSnapshot = {      docs: [{ data: jest.fn().mockReturnValue({ id: '1', message: 'Test notification' }) }]    };    mockFirestore.get.mockResolvedValue(mockSnapshot);    const notifications = await notificationService.getRecentNotifications('user123');    expect(notifications).toEqual([{ id: '1', message: 'Test notification' }]);    expect(mockFirestore.where).toHaveBeenCalledWith('userId', '==', 'user123');    expect(mockFirestore.orderBy).toHaveBeenCalledWith('createdAt', 'desc');    expect(mockFirestore.limit).toHaveBeenCalledWith(20);    expect(mockFirestore.get).toHaveBeenCalled();  });  it('handles notification retrieval failure', async () => {    mockFirestore.get.mockRejectedValue(new Error('Failed to fetch notifications'));    const notifications = await notificationService.getRecentNotifications('user123');    expect(notifications).toEqual([]);  });});