import { AchievementService } from '../achievement_service';import { FirebaseFirestore } from '@firebase/firestore-types';jest.mock('@firebase/firestore-types');describe('AchievementService', () => {  let achievementService: AchievementService;  let mockFirestore: jest.Mocked<FirebaseFirestore>;  beforeEach(() => {    mockFirestore = {      collection: jest.fn().mockReturnThis(),      doc: jest.fn().mockReturnThis(),      set: jest.fn(),      update: jest.fn(),      where: jest.fn().mockReturnThis(),      get: jest.fn(),      runTransaction: jest.fn(),    } as any;    achievementService = new AchievementService();    (achievementService as any)._firestore = mockFirestore;  });  describe('unlockAchievement', () => {    it('calls Firestore set with correct data', async () => {      await achievementService.unlockAchievement('user123', 'ach123');      expect(mockFirestore.collection).toHaveBeenCalledWith('achievements');      expect(mockFirestore.doc).toHaveBeenCalledWith('user123-ach123');      expect(mockFirestore.set).toHaveBeenCalledWith({        userId: 'user123',        achievementId: 'ach123',        unlockedAt: expect.any(Date),        claimed: false,      });    });    it('updates user document with achievement count', async () => {      await achievementService.unlockAchievement('user123', 'ach123');      expect(mockFirestore.collection).toHaveBeenCalledWith('users');      expect(mockFirestore.doc).toHaveBeenCalledWith('user123');      expect(mockFirestore.update).toHaveBeenCalledWith({        achievementCount: expect.any(Object),        lastAchievementAt: expect.any(Date),      });    });  });  describe('syncWithServer', () => {    it('deletes all achievements for the user', async () => {      const mockSnapshot = {        docs: [          { reference: { delete: jest.fn() } },          { reference: { delete: jest.fn() } },        ],      };      mockFirestore.get.mockResolvedValue(mockSnapshot);      await achievementService.syncWithServer('user123');      expect(mockFirestore.collection).toHaveBeenCalledWith('achievements');      expect(mockFirestore.where).toHaveBeenCalledWith('userId', '==', 'user123');      expect(mockSnapshot.docs[0].reference.delete).toHaveBeenCalled();      expect(mockSnapshot.docs[1].reference.delete).toHaveBeenCalled();    });  });  describe('getAchievementName', () => {    it('returns the achievement name from data', () => {      const data = { name: 'Test Achievement' };      expect(achievementService.getAchievementName(data)).toBe('Test Achievement');    });    it('throws an error when data is null', () => {      expect(() => achievementService.getAchievementName(null)).toThrow();    });  });});