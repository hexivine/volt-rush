/// App configuration constants.
class AppConfig {
  static const String appName = 'Volt Rush';
  static const String version = '1.2.0';
  static const int maxRetries = 3;
  static const Duration timeout = Duration(seconds: 30);
}
