import { render, screen, fireEvent, waitFor } from '@testing-library/react-native';import { AchievementsScreen } from '../achievements_screen';import { AchievementService } from '../../services/achievement_service';jest.mock('../../services/achievement_service');describe('AchievementsScreen', () => {  const mockAchievements = [    { id: '1', name: 'Achievement 1', description: 'Description 1', claimed: false },    { id: '2', name: 'Achievement 2', description: 'Description 2', claimed: true },  ];  beforeEach(() => {    AchievementService.prototype.syncWithServer.mockResolvedValue(mockAchievements);    AchievementService.prototype.unlockAchievement.mockResolvedValue(undefined);  });  it('renders loading state initially', () => {    render(<AchievementsScreen />);    expect(screen.getByTestId('loading-indicator')).toBeTruthy();  });  it('renders achievements after loading', async () => {    render(<AchievementsScreen />);    await waitFor(() => {      expect(screen.getByText('Achievement 1')).toBeTruthy();      expect(screen.getByText('Achievement 2')).toBeTruthy();    });  });  it('calls unlockAchievement when claim button is pressed', async () => {    render(<AchievementsScreen />);    await waitFor(() => {      fireEvent.press(screen.getByText('Claim'));    });    expect(AchievementService.prototype.unlockAchievement).toHaveBeenCalledWith('user123', '1');  });  it('handles search functionality', async () => {    render(<AchievementsScreen />);    await waitFor(() => {      fireEvent.changeText(screen.getByPlaceholderText('Search'), 'Achievement 1');    });    expect(screen.getByText('Achievement 1')).toBeTruthy();    expect(screen.queryByText('Achievement 2')).toBeNull();  });});